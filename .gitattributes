# This sourced training input is byte-hashed in the generated model manifest.
# Preserve its archived bytes across checkouts on Windows and Unix.
data/nba-2022-23-heights.json -text whitespace=cr-at-eol
*.pdf binary
